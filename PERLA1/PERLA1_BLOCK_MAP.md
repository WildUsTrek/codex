# PERLA1 Block Map

Last updated: 2026-06-14

This file is the block-level map for safe work on the PERLA1 monolithic runtime. Use it with `PERLA1_PROJECT_MAP.md`, `PERLA1_CONTEXT_BUDGET.md`, `PERLA1_SYMBOL_INDEX.md`, and `PERLA1_TASK_INTAKE_PROTOCOL.md` before planning or patching `01_GIOCO_PRONTO_LOCAL_TEST/index.html`.

This file maps runtime blocks and block ownership risks. It does not duplicate the full agent registry; use `.codex/ORCHESTRATION.md`, `PERLA1_PROJECT_MAP.md`, and `PERLA1_TASK_INTAKE_PROTOCOL.md` for the complete project agent list.

## Purpose

PERLA1 currently uses a single-file HTML/CSS/JS runtime. Work on this kind of monolith must be scoped by functional blocks, not by file names alone.

Rules:

- Treat each block below as a separate ownership area.
- Name the impacted block before changing runtime code.
- Use `PERLA1_SYMBOL_INDEX.md` for orientation, then verify with current code before patching.
- Context limits control reporting volume, not repository inspection depth. Read more of `index.html` when a concrete hypothesis or Team Leader request requires it.
- For long work, track `task_id`, `operation_id`, and `logical_step_id`; call `task-watchdog` when checkpoint or stop conditions are met.
- Do not let two write agents edit the same block in parallel.
- If a task crosses blocks, the Team Leader keeps integration ownership.
- Update this map when a new stable subsystem, contract, validation hook, or recurring risk appears.

## Runtime Blocks

| Block ID | Block | Main Symbols / Areas | Ownership Notes | Required Validation |
| --- | --- | --- | --- | --- |
| `boot-shell` | HTML/CSS/runtime boot | document shell, canvas, UI overlays, `PERLA_BUILD_ID` | Small metadata and boot changes only. Runtime behavior changes must update build id when appropriate. | Page loads through local server, title/build id match. |
| `asset-loading` | Asset manifest and texture loading | `ASSET_BASE`, `ASSET_MANIFEST`, `loadAssets`, `getTex` | Keep paths relative to runtime. Do not rename assets without manifest audit. | Asset load check, console check, missing texture scan. |
| `world-data` | World maps and owners | `map`, `fMap`, `cMap`, `ceilingOwnerMap`, owner ids | Changes affect collision, ceiling cover, roofs, rain, and minimap. Coordinate-dependent validation must use `coordinate_offset_check` when PERLA1 is certain or legitimately suspected. | Deterministic pose check near changed cells, coordinate/effective-pose sanity, minimap sanity. |
| `floor-ceiling` | Floor and ceiling renderer | floor loop in `drawWorld`, ceiling rows, ceiling depth buffers, internal `drawModernCoverCeilingSegment` | Provides internal ceiling/soffit continuity. In V276-V281, external clone/strip/handoff roof visuals are runtime-off and must not be reintroduced as primary roof edges. | Visual check plus internal ceiling/rain-cover sanity where relevant. |
| `wallcasting` | Wall renderer and wall buffers | wall loop in `drawWorld`, `ZBuffer`, `WallTopBuffer`, `WallBottomBuffer`, `WallOwnerBuffer` | Owns solid wall visibility. Changes can break sprites, roof clipping, rain cover. | Visual wall occlusion check, console check, affected debug poses. |
| `roof-system` | Roof and eave system | `roofSegments`, `drawRoofLayer2_5D`, `drawSlopedRoofLayer2_5D`, `drawSlopedRoofGableCaps2_5D`, `drawStableModernOwnerRoofPrimitiveV281`, `perlaStableModernRoofPrimitiveReplacesSegmentV281`, `drawModernIntegratedRoofCapV278`, `perlaModernRoofNearDoorSuppressTopPlanesV280`, V259-V282 helpers | Highest-risk rendering block. V281S is the current runtime roof contract: V281 reception/bath owner 1/2 roofs use a single budgeted world-space primitive authority when preflight accepts the roof, with a narrow owner 1 `back_y0` gable keep for the reception portal/far-closure band so the posterior colmo cannot be culled by rotation. Sloped/gable/V272/V274/V278 are skipped for primitive-owned owner 1/2 roofs; fallback is exclusive when preflight rejects. V282 portal/slab helpers are dormant/off diagnostics unless a future approved task explicitly touches or enables them. Door handling remains based on `roofSegments[].doors`; internal ceiling/rain cover stay separate. | Required roof poses plus `visual_pose_matrix_check`, `roof_visual_matrix_hard_gate`, and for reception owner `1` the `reception_roof_real_objective_gate`: `roof_matrix_declared_before_patch`, current runtime/internal owner envelope from `roofSegments`, HUD/display X recorded separately, `same_coordinate_distance_rotation_grid` with far/close/east/west/interior-or-portal/user-repro groups as applicable, same-coordinate rotations, contact sheet or indexed matrix, `PERLA_BUILD_ID`, `roofV281`, V282 portal/slab counters only when a task explicitly touches or enables V282, primitive pixels/fill rects/budget/warn counters, skipped/suppressed/rejected face counters including owner 1 posterior gable keep/reject, `modernStableRoofPrimitiveHybridViolationV281 === false`, V278 cap pixels/rects zero in normal V281/V281S poses, V266/V267/V273/V274/V275 off counters, pointed-roof/colmo inspection, doorway continuity, fallback sanity where primitive preflight rejects. `matrix_failed_replan_not_ready` if one accepted coordinate loses volume, colmo/ridge/front gable, ceiling authority, or budget stability under rotation. Reception owner `1` readiness additionally requires real visual objective success: high pointed exterior volume, same-coordinate rotation stability, portal/interior flat monocolor ceiling/underside where expected, no exterior roof/gable fragments inside the portal, no fake seams/bands/color-family switches, and user review pause before readiness. |
| `sprites` | Sprite selection and drawing | `sprites`, `getSpriteRenderCandidates`, sprite loop in `drawWorld` | Depends on depth buffers and render order. Avoid broad candidate changes without performance audit. | Candidate/visible/stripe counters, occlusion screenshot. |
| `weather-rain` | Weather and rain rendering | `drawWorldRainParticlesV222`, V246-V257 rain helpers | Rain is sensitive to cover, portals, depth, and camera volume. | Indoor/outdoor transition checks, rain visibility/occlusion counters if exposed. |
| `minimap` | Minimap rendering | `drawMiniMap`, mobile frame-skipping | Keep mobile cost low. Avoid full redraw assumptions. Minimap can contaminate world/render screenshots if it overlaps the target area; use `hud_contamination_check`. | Desktop and mobile viewport sanity, minimap cost if changed, HUD overlap classification when screenshots are used as world proof. |
| `debug-api` | Public debug and QA hooks | `window.__PERLA_DEBUG__`, `perlaLastDrawStats`, `setPlayerForDebug` | Debug hooks are validation infrastructure. Preserve compatibility unless replacing intentionally. Pose-based proof must compare requested/effective pose and offset evidence through `coordinate_offset_check`. | Existing debug calls still work; coordinate/effective-pose sanity for pose-based validation. |
| `performance-observability` | Performance counters and runtime stats | draw/fps/cache counters, `perlaLastDrawStats`, roof, V277 continuity fill, V278 integrated cap, V279/V280 cap profile/support-span/door-span counters, V281 primitive counters, eave handoff, and sprite summaries | Provides proof for performance-sensitive work. Do not replace measured counters with visual guesses. | Before/after counters for changed hot paths; V281 roof-visible poses must keep primitive below warn/budget across the accepted `visual_pose_matrix_check`, V278 cap zero, fallback paths exclusive, and failed replacement paths off. |
| `launcher-server` | Windows launcher and local server | `AVVIA_GIOCO_WINDOWS_SENZA_PYTHON.bat`, `AVVIA_GIOCO_SERVER_POWERSHELL.ps1` | This is the required runtime validation startup path. Keep path-relative and user-friendly. Do not hard-code usernames. V278 launcher avoids unbounded pre-bind port cleanup and uses bounded old-server detection only after a bind failure. | Launcher starts or reports a clear occupied-port error for `http://127.0.0.1:8000/`. |
| `tooling-ci` | Static analysis, dependency graph, local CI, modularization scaffolding, deterministic workflow checks, and project backup tooling | `tools/perla_runtime_analyzer.mjs`, `tools/perla_local_ci.ps1`, `tools/perla_codex_workflow_check.ps1`, `tools/perla_project_backup.ps1`, `tests/perla_regression_suite.json`, `PERLA1_MODULARIZATION_PLAN.md`, `01_GIOCO_PRONTO_LOCAL_TEST/src/module-boundaries.json`, `01_GIOCO_PRONTO_LOCAL_TEST/src/README.md` | Tooling may inspect the full monolith and workflow docs and emit generated reports, JSON, JSONL, or zip backups, but must not change runtime behavior. `perla_project_backup.ps1` backs up the full synchronized repository folder `C:\Users\ASUS\Documents\GitHub\codex`, excludes only `PERLA1\01_GIOCO_PRONTO_LOCAL_TEST\assets\rtp`, writes zip files outside Git, and applies automatic retention only to files in `backup\automatici`. `workflow_tooling_manifest` separates tracked tooling from disposable evidence. `src/` remains scaffold-only until a scoped extraction is validated. `safe-fixer` may patch this block only with explicit non-runtime tooling scope. | Local static CI passes; workflow check returns no P0/P1 failures; backup tool dry run or execution reports schema `perla.project_backup.v1`; generated reports/JSONL/backup zip files are ignored unless intentionally promoted. |
| `sync-docs` | Repository sync and technical docs | parent sync scripts, root `.codex/hooks.json`, `AGENTS.md`, `PERLA1_PROJECT_MAP.md`, this file, `PERLA1_CONTEXT_BUDGET.md`, `PERLA1_SYMBOL_INDEX.md`, `PERLA1_TASK_INTAKE_PROTOCOL.md`, `.codex/` | Rule changes must not silently weaken validation or write boundaries. Hooks are lifecycle checks only and must not replace required `CALL` agent output; `hook_trust_check` is needed before relying on hooks as active enforcement. `checker_semantic_limit` must stay explicit because deterministic checks are not full semantic/runtime proof. Agent invocation changes must preserve `call_agent_evidence`, `direct_invocation`, `generic_adapter`, `tooling_blocked`, `user_delegation_state`, and `visual_qa_required`; `critical_path` cannot become a generic bypass for required agents. Complex Task Accelerator changes must preserve `accelerator_brief`, `cheapest_discriminating_test`, `critical_path`, `sidecar_tasks`, `serial_constraints`, `validation_ladder`, `checkpoint_ledger`, and `subagent_task_packet` without bypassing gates. Sidecar Result Integration changes must preserve `sidecar_result_integration`, `result_status`, `affects_critical_path`, `dependency_on_critical_path`, `accepted_into_plan`, `integration_decision`, `validation_impact`, `write_scope_impact`, `approval_impact`, `heartbeat_checkpoint`, `stop_condition_triggered`, `discard_or_defer_reason`, and `ledger_update` without turning evidence into approval. User Intake Relay changes must preserve `user_message_intake`, `message_class`, `must_interrupt`, `must_report_to_team_leader`, `checkpoint_required`, `user_intent_summary`, `conflicts_current_plan`, `changes_scope`, `changes_validation`, `changes_write_scope`, `approval_impact`, `agent_gate_impact`, `agent_tool_mapping_impact`, `critical_path_impact`, `sidecar_integration_impact`, `decision`, `ledger_update`, `relay_note`, `next_action`, `forbidden_next_action`, and `response_due` without promising persistent background monitoring or granting relay approval authority. `project_backup_gate`, `backup_user_requested`, `automatic_task_backup`, `scoped_finalization`, `finalization_gate`, and `subagent_task_lifecycle` must prevent missing backups, global staging, unrelated cleanup, and stale subagent records from being confused with active work. | Rules remain consistent; no circular agent authority; workflow check returns no P0/P1 failures; finalization separates in-scope, out-of-scope, generated, untracked workflow tooling, and project backup status before sync/readiness claims. |
| `reports-history` | Historical reports and snapshots | `report/`, extracted scripts, smoke outputs | Read for evidence only. Do not patch runtime through extracted scripts. | Current runtime verification wins over historical report assumptions. |

## Block Ownership Rules

- `safe-fixer` may patch runtime blocks only after the impacted block is named and the diagnosis is clear. It may patch `tooling-ci` files only when the approved scope explicitly names those files or that block.
- `workflow-consistency-auditor` audits `sync-docs` read-only for hierarchy, permission, gate, TOML, and source-of-truth conflicts. P0/P1 findings must stop the next operational step until fixed or approved.
- `plan-integrity-auditor` audits selected plans read-only before execution when `sync-docs`, runtime contracts, validation routes, rollback/refactor scope, or multi-step implementation plans are involved.
- `refactor-surgeon` may touch the minimum demonstrably sufficient files/symbols only after documented blockage and approved scope. It may cross blocks only when the failure brief proves coupling and the Team Leader/user approval covers that scope.
- `map-maintainer` may update `PERLA1_PROJECT_MAP.md`, `PERLA1_BLOCK_MAP.md`, `PERLA1_CONTEXT_BUDGET.md`, `PERLA1_SYMBOL_INDEX.md`, and `PERLA1_TASK_INTAKE_PROTOCOL.md` only. It must not edit runtime code, `.codex/config.toml`, `.codex/agents/*.toml`, `.codex/ORCHESTRATION.md`, or `AGENTS.md`.
- Work on `sync-docs` requires a governance preflight: `workflow-consistency-auditor` and `workflow-guard` are `CALL` before patching or syncing policy changes. If named agents are unavailable but generic subagents exist, use the Agent Tool Adapter Rule before declaring `TOOLING_BLOCKED`.
- Work on `tooling-ci` must keep generated reports, JSON, JSONL, hook logs, and one-off Codex exec transcripts out of normal Git churn unless the user explicitly asks to promote an artifact. Tooling changes do not validate rendered behavior unless runtime screenshot/counter validation also runs. Screenshot/counter validation must carry `hud_contamination_check` and `coordinate_offset_check` when the evidence depends on PERLA1 pose/location.
- Work on `sync-docs` or `tooling-ci` must run `scoped_finalization` before final delivery or sync when the worktree is dirty: classify approved scope, out-of-scope changes, generated/disposable evidence, untracked workflow tooling, validation run, selective staging state, and residual risk.
- `scoped_finalization` is classification only. It does not authorize rollback, deletion, cleanup, or staging of unrelated dirty work.
- Read-only auditors may inspect any block but must not propose broad rewrites as the first fix.
- `skeptic-auditor` may inspect any block read-only to challenge the current plan and propose alternatives; it must not approve write work as complete.
- Extra temporary agents start read-only unless the Team Leader assigns an explicit disjoint write scope.
- A write agent cannot approve its own patch as validated. Validation must come from the Team Leader or a separate read-only auditor.

## Cross-Block Risk Map

| Source Block | Commonly Affected Blocks | Risk |
| --- | --- | --- |
| `floor-ceiling` | `roof-system`, `wallcasting`, `weather-rain` | Ceiling continuity changes can create fake visible roof edges or cover errors. V276/V281 preserve internal ceiling but do not use external ceiling clones as roof visuals. |
| `wallcasting` | `roof-system`, `sprites`, `weather-rain` | Wall depth and owner buffers drive clipping and occlusion. |
| `roof-system` | `floor-ceiling`, `wallcasting`, `sprites`, `performance-observability` | Roof fixes can create slab fills, dotted borders, clone-suppression gaps, or draw-cost spikes. |
| `sprites` | `wallcasting`, `floor-ceiling`, `performance-observability` | Candidate expansion can overdraw or break occlusion. |
| `asset-loading` | all visual blocks | Missing or renamed assets can look like renderer failures. |
| `launcher-server` | validation workflow | If the launcher/server is not running first, browser validation fails before testing the runtime. Wrong route or cache can validate a stale build. |

## Minimum Block Handoff

Any agent reporting on a block must return:

- block id,
- files read,
- read scope,
- exact symbols or line hints,
- risk level,
- proposed next action,
- validation required,
- unknowns marked as `Da indagare`.
